---
title: "Kimmo Suokas"
---

```{r}
library(magrittr)
library(data.table)
library(gt)

edu <- fread("../data_personal/education.csv")
pos <- rbindlist(list(
  fread("../data_personal/positions_other.csv"), 
  fread("../data_personal/positions_clinical.csv")
))
awards <- fread("../data_personal/awards.csv")

```

Last updated: `r format(Sys.Date(), '%d.%m.%Y')`

## Current work

```{r}

a <- data.table(years = 2023, what = '**Researcher**, [Psychosocial Factors and Health - study group](https://www.helsinki.fi/en/researchgroups/psychosocial-factors-and-health)', where = 'Department of Psychology, Faculty of Medicine, University of Helsinki.')


b <- data.table(years = 2023, what = '**Consultant Psychiatrist**', where = '[Mehiläinen](https://www.hillaoy.fi)')

c <- data.table(years = 2019, what = '**Member of the ICD-11 Finnish translation core group**', where = '[Finnish Psychiatric Association](https://www.psy.fi/in_english)')

rbindlist(list(a, b, c), idcol = "item") %>% .[,.(years = paste0(years, " ->"), what =paste0(what, " <br> ", where))] %>% 
  gt() %>% 
   fmt_markdown(columns = everything()) %>% 
  tab_options(
    table.width = pct(100),
    column_labels.hidden = TRUE,
    table.border.top.style = "hidden",
    table_body.hlines.style =  "hidden",
    table_body.border.bottom.style = "hidden"
  ) %>% 
   cols_width(
    years ~ px(120)
) %>% 
  cols_align(
    align = c("left"),
    columns = everything()
) %>% 
    tab_style(
    style = "vertical-align:top",
    locations = cells_body(
      columns = years
      )
  ) 
```

## Education

```{r}

edu[order(-end_date)] %>% .[language=="en", .(years = paste0(end_date %>% year()), what = paste0("**", what, "**, ", where))] %>%
  gt() %>% 
   fmt_markdown(columns = everything()) %>% 
  tab_options(
    table.width = pct(100),
    column_labels.hidden = TRUE,
    table.border.top.style = "hidden",
    table_body.hlines.style =  "hidden",
    table_body.border.bottom.style = "hidden"
  ) %>% 
   cols_width(
    years ~ px(120)
) %>% 
  cols_align(
    align = c("left"),
    columns = everything()
)
```

## Awards

```{r}

# list(
#   data.table(years = 2025, what = '**Martti Kaila Award** for an Outstanding Doctoral Dissertation in 2024 by the [Finnish Psychiatric Association](https://psy.fi/martti-kaila-palkinto-2024/)'),
#   data.table(years = 2025, what = 'University of Tampere Foundation Grant for a doctoral dissertation accepted with distinction 2025'),
#   data.table(years = 2021, what = '[Research Prize](https://www.europsy.net/previous-winners/) by the European Psychiatric Association (EPA)'),
#   data.table(years = 2020, what = '**Publication of the Year** award by the Finnish Psychiatric Association')) %>% 
#   rbindlist() %>% 
awards[order(-start_date), .(years, item = paste0("**", what, "** by ", awarding_body))] %>% 
  gt() %>% 
   fmt_markdown(columns = everything()) %>% 
  tab_options(
    table.width = pct(100),
    column_labels.hidden = TRUE,
    table.border.top.style = "hidden",
    table_body.hlines.style =  "hidden",
    table_body.border.bottom.style = "hidden"
  ) %>% 
   cols_width(
    years ~ px(120)
) %>% 
  cols_align(
    align = c("left"),
    columns = everything()
) %>% 
  tab_style(
    style = "vertical-align:top",
    locations = cells_body(
      columns = years
      )
  ) 


```

## Past work

```{r}

add_link <- function(text, url) fifelse(!is.na(url), paste0("[", text, "](", url, ")"), text)

a <- pos[order(-end_date)][language=="en" & !is.na(end_date), .(years = ifelse(year(start_date) == year(end_date), year(start_date),  paste0(year(start_date), "--", year(end_date))), item = paste0("<b>",what, "</b><br>", where, "<br>", details,
        fifelse(!is.na(url), add_link(" link", url), "")                                                                                              ))] %>% head(-6) 
b <- data.table(years = c("2011--2018"), item = paste0("**Specialising Physician** \n\n  ", pos[language == "en"] %>% 
  tail(6) %>% 
  .$where %>% 
  paste(collapse = '; ')))

rbindlist(list(a, b)) %>% 
  gt() %>% 
   fmt_markdown(columns = everything()) %>% 
  tab_options(
    table.width = pct(100),
    column_labels.hidden = TRUE,
    table.border.top.style = "hidden",
    table_body.hlines.style =  "hidden",
    table_body.border.bottom.style = "hidden"
  ) %>% 
   cols_width(
    years ~ px(120)
) %>% 
  cols_align(
    align = c("left"),
    columns = everything()
) %>% 
  tab_style(
    style = "vertical-align:top",
    locations = cells_body(
      columns = years
      )
  ) 



```
